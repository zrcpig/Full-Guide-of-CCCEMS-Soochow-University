# ── 文本文件统一使用 LF 行尾，避免跨平台 CRLF 引入幽灵 diff ──
*.md    text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
*.py    text eol=lf
*.txt   text eol=lf
*.css   text eol=lf
*.js    text eol=lf
*.json  text eol=lf
*.html  text eol=lf
*.svg   text eol=lf
*.toml  text eol=lf
*.cfg   text eol=lf

# ── Git LFS：显式指定的大体积 PDF 走 LFS ──
# 注意：历史中绝大多数 PDF 是以原文件形式直接入库的（早期 .gitattributes 的
# “*.pdf” 用了全角引号导致通配未生效）。为避免对历史文件触发大规模 LFS 转换，
# 这里不使用 *.pdf 通配，仅对新增的大 PDF 按需追加显式条目。
高分子化学第5版[[:space:]]导读与题解[[:space:]]潘祖仁[[:space:]]课后习题思考题计算题答案.pdf filter=lfs diff=lfs merge=lfs -text
高分子化学(第五版)潘祖仁版配套习题书.pdf filter=lfs diff=lfs merge=lfs -text
Atkins_ Physical _Chemistry_11th_cn.pdf filter=lfs diff=lfs merge=lfs -text

# ── 二进制资源，不做行尾转换 ──
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.mp3  binary
*.mp4  binary
*.pptx binary
*.doc  binary
*.docx binary
*.zip  binary
